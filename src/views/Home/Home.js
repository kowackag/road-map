import React from 'react';
import PropTypes from 'prop-types';
import StyledTitle from 'components/Title.styled';

const Home = () => {
  console.log(window.H)
  return <StyledTitle>Recently searched</StyledTitle>;
};
Home.propTypes = {};
export default Home;
